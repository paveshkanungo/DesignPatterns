package org.example.Structural.Composite;

public class Definition {
    void definitionComposite(){
        /*
        *   The Composite Design Pattern allows you to compose objects into tree structures to represent
            a part-whole hierarchies which lets clients manage and perform operations on individual
            objects(leaf nodes) and compositions of objects(composite nodes) uniformly(treating both
            objects the same way i.e., without knowing whether it's working with a component or a composite).
        *
        *   Example:-
        *   File System,
        *   Calculator
        *
        * */
    }
}
